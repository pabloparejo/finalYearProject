(function(){
	'use strict';
	angular.module('files.controllers', [])

		// Workers Controllers
		.controller('Controller', ['$scope', '$filter', function($scope, $filter){
				
			}
		])
	}
)()